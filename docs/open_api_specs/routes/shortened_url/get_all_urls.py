specs = {
    "description": (
        "Returns all user's shortened URLs."
    ),
    "responses": {
        200: {
            "description": "Shortened URLs returned successfully.",
        },
    }
}
